# Full-kit animation — reviewed Kogan batches, September 5, 2026

Kogan previously held one hop drawing and one full-jump drawing through each arc. The full-jump silhouette also shrank sharply relative to neutral. Eight new drawings now show takeoff compression, distinct hop and full-jump rise/apex/descent, and landing compression. The large copper cape and complete saber stay visible. Anatomical scale is calibrated independently from the bent silhouette and air root.

Selection follows the existing action and vertical velocity. It adds no simulation state or timing. Hops still land freely; normal full jumps retain 2f recovery. The first landing tick compresses, the second uses the existing rising pose. Uppercut descent keeps its dedicated reversal drawing. A frame-by-frame review caught duplicate airborne silhouettes during the old crossfade; movement transitions now cut between authored drawings.

## Reproduce the review

```sh
cargo run --release -p aeon -- --kit-preview --kit-movement
cargo run --release -p aeon -- --kit-preview --kit-movement --kit-legacy-movement
cargo run --release -p aeon -- --kit-preview
cargo run --release -p aeon -- --kit-preview --kit-raya
```

Movement has 24 neutral/forward/backward hop/full-jump cases, both facings at center/corner. The default lights preview has 60 standing P/K and crouching K cases covering hit, standing guard, crouching guard, crouched hit and whiff. `--kit-case=N` isolates a case. SPACE pauses, period steps, ESC exits. `--capture` writes every 60 Hz tick to PNG plus a trace and case index in `shots/kit`. Run captures from a fresh directory to preserve earlier evidence. These full-resolution PNGs consume several GB; retain video, traces and selected diagnostics before removing redundant generated frames. The existing `--polish-preview` remains 30 fps / 2,100 simulation ticks.

## Benchmarks and refraction

The initial study visually inspected two timestamped excerpts per requested game at 1×, then selected paused phases. Recordings remain on their original hosts. No exact game-frame counts are inferred from video rate or compression.

| Game and source | Inspected excerpts and observation |
|---|---|
| [Garou: Mark of the Wolves, PS4 Rock arcade recording](https://www.youtube.com/watch?v=4-6_vD9NsSk&t=376s) | 6:16.47–6:17.40 changing knee/arm arrangement through movement; 6:17.40–6:18.10 short strike, recoil and arm withdrawal. |
| [Samurai Shodown V Special, PS4 Ukyo arcade recording](https://www.youtube.com/watch?v=jKfB-HPhPZs&t=186s) | 3:06.25–3:07.65 committed blade line and withdrawal; 3:07.52–3:08.82 gathered jump legs with distinct cloth/weapon silhouette. |
| [KOF XIII jump tutorial, Hit Box Arcade](https://www.youtube.com/watch?v=htMZ7Pi7udY&t=72s) and [Kyo versus Kim recording](https://www.youtube.com/watch?v=siJoDHxttqQ&t=82s) | 1:12.38–1:13.63 compression, gathering and extension; 1:22.43–1:23.13 kick chamber, support leg, recoil and foot replacement. Original-era XIII, platforms unverified; tutorial inset supports gross poses only. |
| [Original Guilty Gear XX Accent Core, GVN winner finals](https://www.youtube.com/watch?v=vSESVFomvWA&t=83s) | 1:23.78–1:25.05 A.B.A key pivot and recovery while Testament tumbles; 1:26.38–1:27.18 recognizable body/equipment through jump and broad effects. [Organizer's 2007 post](https://archive.supercombo.gg/t/gvn-6-23-3s-ggac-results/26095) identifies this edition; low-quality display recording supports silhouette observations only. |

The Kogan movement batch applies the Garou/KOF joint changes through Aeon's painted copper/cyan identity: compressed takeoff, gathered knees, legs reaching toward the floor and cape follow-through. It preserves the short tucked hop versus extended full jump and Aeon's own landing rules. No borrowed sprites, movesets, chains, mobility or timings are introduced. This is a focused comparison, not a claim of commercial-game parity.

## Evidence and limits

Legacy movement was viewed at 1× around 0–3.69 and 12–14.80 seconds. All 24 seconds of the first integration were viewed at 1× through temporal samples. Selected full-jump descent/landing frames at 12.90–12.983 seconds revealed the blend defect. The final correction was viewed at 1× around 0–6.2 and 12.24–16.15 seconds, then stepped at 0.633–0.683 and 12.933–12.983 seconds. These show clean floor contact without a second body. Both facings and corner cases retain the complete blade/cape and clear gameplay HUD. The diagnostic case title can overlap an apex; it is not gameplay HUD.

119 workspace tests pass (89 sim, 30 client), clippy passes with warnings denied, and the release build passes with locked/offline dependencies. The 1,440-tick movement trace matches legacy byte for byte. The retained 2,100-tick polish trace also matches af92c10 after the blend correction. Final smoke images are byte-identical to the eight inspected images. Simulation sources and authored frame data are unchanged. Title/select/winner, versus and training smoke screenshots were inspected. Physical arcade-stick play and competitive acceptance remain pending.

This is one reviewed movement batch. Air attacks, other move families and Raya's movement remain open. A dedicated crouched-hit baseline confirms Raya's lowered reaction, but also exposes Kogan's legacy CrK punch drawing, clipped rear blade and excessive overlap. Older colored fringes, pose holds and Raya ascension HUD clearance remain in the full-kit defect ledger. Full-kit completion is not claimed.


## Grounded revolver, wave and EX batch

Eight new V5 drawings cover Kogan ShotA/ExB preparation, hip-level firing, compact follow-through and lowering, plus ShotB lift, low cut, withdrawal and ready. The weapon remains complete throughout. Selection reads existing action frames; authored poses cut cleanly and receive no extra procedural rotation/stretch. AirShot keeps its existing art and remains open. No combat numbers, projectile geometry or simulation sources changed.

```sh
cargo run --release -p aeon -- --kit-preview --kit-ranged
cargo run --release -p aeon -- --kit-preview --kit-ranged --kit-legacy-ranged
cargo run --release -p aeon -- --kit-preview --kit-ranged --capture --capture-1x
```

The 48 cases use legal motions and test ShotA/ShotB/ExB against hit, standing guard, crouching guard and miss, both facings at center/corners. Each case lasts 150 ticks. Timed jumps make gun shots miss; spacing makes waves expire. `--capture-1x` selects 1280×800 output instead of Retina resolution, preserving every simulation tick. Default game resolution behavior is unchanged. The diagnostic title now sits below the arena, clear of jump apexes.

SSV Special's committed weapon line/withdrawal and original Accent Core's equipment/effect hierarchy informed this batch. Legacy gun art arrived already extended with an empty saber hand; legacy wave art held its cut and painted slash effect throughout release. V5 distinguishes preparation, release and recovery while the live projectile separates from the body. Aeon's gun deliberately uses a compact hip shot at its authored flight height. Neither source is treated as a gun-mechanics reference. Comparison bodies are about 210 pixels high in the review player; AC evidence supports gross silhouette only.

Baseline 1× review covered approximately 0–3.8, 40–44.4 and 80–83.8 seconds. V3 first integration was viewed at 0–10 and 40–45, then gun phases .317/.367/.417/.583/.750 were paused. This exposed its high muzzle above the bullet and failed visual acceptance. V4 was archived without integration; V5 lowered the arm and corrected extraction margins. Focused V5 gun/wave cases were each viewed through 2.5 seconds at 1×. Paused gun ticks 24/25/49 and wave ticks 25/26/29/34/43 verify muzzle alignment, blade clearance and withdrawal.

The entire final V5 120-second capture was played at 1× in contiguous passes, with temporal samples through all 48 cases. Additional 80–82.395 seconds follows EX contact, down hold and getup. Paused normal crouch-block tick 34 and EX release ticks 21–23 confirm guard/contact and belt-level muzzle alignment. Both facings and corners retain cape, weapons, floor anchors and clear gameplay HUD. Raya's older reaction/getup crossfades remain open; this batch accepts the three Kogan actions, not the whole victim family.

Final verification: **120 workspace tests (89 sim + 31 client)**, clippy with warnings denied, and locked/offline release pass. The final 7,200-tick ranged trace is byte-identical to legacy/V3. The retained 2,100-tick polish trace matches the movement baseline and af92c10. Eight final smoke hashes match the eight inspected movement screenshots, including versus and training. Complete before/V3/final videos, traces, case indexes and selected PNG diagnostics remain in the vault review package. Exact prompts and selection history are in `crates/client/assets/animation/RANGED-2026-09-05.md`. Physical stick play and remaining full-kit coverage remain pending.

## Cape-snare and threshold-step batch

Eight V3 drawings replace held grab/dash silhouettes. Cape-snare prepares, reaches low, releases and withdraws; threshold-step gathers, glides, brakes and settles. The complete saber stays in hand and returns to the established low front line before idle. The cape stays large, with just two narrow forward streamers so the opponent's face remains visible. Selection reads existing frames/connection; matching-cell trails prevent an old stance or leaning head appearing ahead of the braking body. No extra rotation/stretch or crossfade is added to these drawn phases.

```sh
cargo run --release -p aeon -- --kit-preview --kit-utility
cargo run --release -p aeon -- --kit-preview --kit-utility --kit-legacy-utility
cargo run --release -p aeon -- --kit-preview --kit-utility --capture --capture-1x
```

The 28 cases cover command-grab hit, standing/crouching guard, distant miss and legal jump escape, plus near-opponent and unobstructed step travel, both facings at center/corners. Each lasts 150 ticks. Existing 6/2/38 grab timing, 4f command hold and 16f step with 12f travel remain unchanged; the cape is purely visual.

A1/G2's separate attacker withdrawal and victim consequence, S2's recognizable weapon return, and G1's weight gathering inform the gestures. Aeon keeps its own low cape reach and grounded glide. Legacy review at 1× covered 0–3.803, 49.803–53.938 and 59.938–63.488 seconds; it showed a shrinking held reach, face-obscuring curls and a held lunge with old-body trails. V2 lowered the reach, but focused 1×/frame-step review exposed different-cell trails and backward ending blades. V3 plus the trail filter correct those defects.

The complete final 70 seconds was played at 1× in contiguous passes, with temporal screenshots through all cases. Paused grab ticks 23/40/58/59 and step ticks 13/16/21/25/28/29 verify release, complete weapons above the floor, stable scale, brake and clean ready/idle cuts. Near-opponent overlap preserves faces; jump escape leaves health unchanged. The final cape has a small fold change at idle. Raya's older reaction/landing/getup crossfades remain separate open coverage.

Final verification: **121 tests (89 sim + 32 client)**, clippy with warnings denied and locked/offline release pass. The full 4,200-tick utility and retained 2,100-tick polish traces match their baselines byte for byte. All eight final smoke hashes match previously inspected versus/training/title/select/winner images. Complete legacy/V2/final videos, traces, case indexes and 240 final diagnostics are retained in the vault review package. Exact prompts/variant history are in `crates/client/assets/animation/CAPE-STEP-2026-09-05.md`. Remaining kit families and physical stick play are open.

## Saber, rekka and reversal batch

Kogan's legacy StS drew a low sweep despite its high hitbox; equal-grid cut extraction imported neighboring cape fragments, and crossfades left extra sabers/heads over recovery. Four new short high-poke drawings and measured original cut/backcut regions fix those mismatches. Authored combat phases now cut cleanly and trail only matching drawings, without additional rotation/stretch. The existing full-thrust drawings remain exclusive to the final rekka.

The tall uppercut pose previously clipped the viewport and timer near the apex. Two new compact airborne drawings finish the frontward 6→3→12 saber path early, then gather knees/hips and lower the saber through the high arc. Original coil/rise/descent and existing taxed landing remain. A fixed 128-world-pixel wall allowance also keeps the long thrust within both corners; zoom, collision walls, trajectory, move values and inputs are unchanged.

```sh
cargo run --release -p aeon -- --kit-preview --kit-saber
cargo run --release -p aeon -- --kit-preview --kit-saber --kit-move=Rekka3
cargo run --release -p aeon -- --kit-preview --kit-saber --kit-move=Uppercut --kit-case=0 --capture --capture-1x
```

The 128 cases cover StS, StHS, StHSClose, Rekka1/2/3, ExA and Uppercut against hit, standing guard, crouching guard and miss, in both facings at center/corners. Later rekkas use real follow-up presses after active frames, respecting hitstop. Close-HS misses use a near jump escape so the input retains the close selector. StS and Uppercut pass above crouched Raya under the existing geometry. `--kit-move` filters before `--kit-case`. Each case lasts 150 ticks, or 180 for the full three-hit chain and recovery.

S2/A1 commitment and equipment return inform the clean weapon phases; G2/S2 inform the short StS pulse and withdrawal. S1/A2/K1 equipment continuity and gathered joints inform the uppercut. Aeon deliberately completes the tall gesture early and holds a low compact apex to keep the HUD readable. References were already inspected at 1× and selected phases stepped; small KOF/off-display AC footage supports gross silhouette observations only. No source sprites, costumes, timings or mechanics are copied.

Focused legacy StS (2.5s), Rekka3 (3s) and Uppercut (2.5s) were viewed completely at 1× and compared with corrected captures. Paused legacy/final thrust ticks 64/85 show the removed double saber/head. Legacy uppercut tick 47 shows the HUD clip; corrected ticks 30/34/47/61/71/72/83 show high follow-through, gathered apex, descent and clean landing. A complete first candidate exposed the low StS and imported cape fragments. A later full Rekka3 corner review exposed viewport clipping; neither candidate was treated as accepted.

All eight final `saber-framed-*` clips were played completely at 1× in a fixed 1280×720 viewport, with temporal samples across all 128 cases: seven 40-second clips and Rekka3 at 48 seconds, totaling 328 seconds / 19,680 ticks. Short StS keeps a high blade above crouched Raya, and paused ticks 37/39 show withdrawal. Final corner thrust contact/hold/withdrawal at case 2 ticks 64/74/85 preserves the full tip. Complete reversal playback and repeated paused phase boundaries keep blade/cape below the timer and round label. ExA reuses the first-cut phases at its own authored interval with existing flash and gauge use. Same-cell displaced trails remain restrained; old pose ghosts are removed from these Kogan transitions.

Final verification: **125 tests (89 sim + 36 client)**, clippy with warnings denied and locked/offline release pass. Actual keyed-art regressions cover cell boundaries, uppercut HUD clearance and thrust corner clearance. All 19,680 final saber ticks match the first candidates byte for byte; focused before/after traces match too. The retained 35-second eight-scene preview was viewed completely at 1× and its 2,100-tick trace matches the prior baseline. Eight fresh title/select/winner, versus and training smoke screenshots were individually inspected after framing changed the backdrop. Simulation and frame-data sources are unchanged.

Complete before/candidate/final videos, traces/case indexes, 2,416 final saber diagnostics, 71 polish diagnostics and eight smoke screenshots are retained in the vault review package. Exact prompts and originals are documented in `crates/client/assets/animation/KOGAN-STANDING-POKE-2026-09-05.md` and `KOGAN-UPPERCUT-COMPACT-2026-09-05.md`. Raya's older reaction/landing/getup/glyph ghosts, Kogan's remaining air/light families and fine legacy fringes stay open. This accepts eight Kogan actions; full-kit completion and physical stick play remain pending.

## Disc-shield batch

Four V2 drawings replace Kogan's held kneeling disc: gathered small ring, committed open shield, dismissal while rising, and settle without the shield. Shared anatomical scale keeps the kneeling head/body coherent with standing. The complete saber remains above the floor and returns to the familiar low front line. V1's two blade tips fell below the boots; a built-in imagegen edit raised those blades before integration. Exact prompts, original IDs and measurements are in `crates/client/assets/animation/KOGAN-DISC-2026-09-05.md`.

The full disc is selected only during the existing active span; the two recovery drawings divide the authored recovery. Clean combat cuts and matching-cell trails remove the old shield/vertical-saber overlay at idle. No extra rotation/stretch, timing, projectile geometry, input or simulation change is introduced. Disc remains 8/5/12.

```sh
cargo run --release -p aeon -- --kit-preview --kit-disc
cargo run --release -p aeon -- --kit-preview --kit-disc --kit-case=16 --capture --capture-1x
```

The 20 legal-input cases cover close hit, standing guard, crouching guard, spaced miss and actual Raya glyph absorption, both facings at center/corners, 90 ticks each. The fixture verifies expected hit/block/miss, one absorbed glyph without damage, all four drawings and both fighters' recovery. The existing disc-versus-voice trial supplies cast/answer timing.

S2's clear commitment/withdrawal and A2's equipment/effect hierarchy inform this batch. Aeon deliberately uses an open copper ring so the opponent remains visible, with a quiet gathered start and recognizable dismissal. These observed principles are refracted through Aeon's defensive gesture; neither source is claimed as a matching disc mechanic.

Complete legacy and final 30-second clips were viewed at 1× in a fixed 1280×720 player, with temporal screenshots through all 20 cases. Legacy held a solid shield throughout and left a shrinking body and old shield over idle. Final playback shows the brace, small-ring dismissal and rising return, full cape and weapon at both corners. Paused final projectile case 16 at ticks 12/13/20/24/27/33/40 (24.200/.217/.333/.400/.450/.550/.667 seconds) separates the preceding crouch input, preparation, approaching glyph, absorption, dismissal, settle and idle. Crouch-block case 8 tick 24 (12.400s) keeps Raya's hood/face and cloth visible through the open rings. Contact still uses the existing live effect cue. The legacy crouch-entry ghost before the special starts and Raya's reaction/glyph ghosts remain open in their own families; the disc-to-idle ghost is removed.

Final verification: **126 workspace tests (89 sim + 37 client)**, clippy with warnings denied and locked/offline release pass. The complete 1,800-tick disc trace is byte-identical to baseline. The retained 2,100-tick integration trace matches the preceding saber build; its complete 35-second encoded video is also byte-identical to the fully inspected saber integration video (SHA-256 `c96fbe1a5b344cf4f6eeecadc9e78e765345a5037e2e484d43b10175b4bd68e9`). All eight fresh smoke images are byte-identical to the inspected saber screenshots, including versus and training. These exact comparisons reuse the preceding visual review, rather than claiming a new viewing.

Complete before/final videos, traces/cases, 320 diagnostics each, retained polish evidence and smoke images are archived in the vault review package. Redundant own raw PNGs were pruned only after complete videos/traces and selected diagnostics existed. Remaining Kogan/Raya families and physical stick play remain open; the full-kit goal continues.


## Ground movement batch

Kogan's old run leaned excessively and clipped equipment; backdash looked like a forward lunge and left a second body at idle. Walking imported a neighboring cape fragment, crouch dropped/rose abruptly with ghosts, and close crouch hid his entire head behind Raya's linen. Eight V4 drawings and measured original walk regions correct these defects. Run uses a gathered entry, two changing cape keys with fixed leg arrangement, then brake/settle. Crouch compresses through a half drawing and deliberately holds low; release rises through the half drawing. Retreat gathers, shifts weight backward, brakes and settles within its existing 14f.

Phase age is client history only. Same-frame redraw, hitstop and RC hold the clock; replay/training resets discard it. Any new action overrides ground settling immediately. No input, recovery, move value, hitbox, trajectory or simulation source changed. At equal height, a sole crouching body and Kogan's brief half rise draw in front; attacking gestures retain priority and airborne order stays intact. This keeps both faces visible during close overlap.

```sh
cargo run --release -p aeon -- --kit-preview --kit-ground
cargo run --release -p aeon -- --kit-preview --kit-ground --kit-ground-state=RunCrouch
cargo run --release -p aeon -- --kit-preview --kit-ground --kit-ground-state=BackDash --kit-case=2 --capture --capture-1x
```

The 36 legal-input cases cover WalkForward, WalkBack, Crouch, RunStop, RunCrouch, RunBlock, RunJump, RunAttack and BackDash, both facings at center/corners, 90 ticks each. Walking covers two complete cycles. Run exits begin on input frame 40; real block/StS contact and complete full-jump landing are verified. State filters apply before `--kit-case`. Retreat uses the wall behind the moving body; forward-run cases use the opponent's wall.

G1/K1 body gathering and weight transfer, S1 equipment/cloth continuity and S2 recognizable return inform the batch. Aeon deliberately retains a grounded glide and immediate exits. A2 visibility informs the close-crouch overlap correction. The already inspected normal-speed reference excerpts and paused phases are reused; no source mechanics, frame counts or artwork are imported.

All nine before and final clips were played completely at 1× in a fixed 1280×720 player: 54 seconds / 3,240 ticks each, with temporal screenshots across all 36 cases. Neutral bodies are about 210px high. A focused V4 candidate still obscured the crouched face at ticks 45/56; final drawing order fixes that. Final RunCrouch ticks 41/43/67/69 show half entry, low hold, visible rise and neutral. RunStop ticks 15/17/25/33 show gather and changing cape keys; 41/43/45 show brake/settle/idle. BackDash ticks 15/18/24/27/29 show preparation, backward travel, brake, settle and clean idle. Full weapons and capes stay above the floor and inside both corners. Same-cell displaced travel trails are deliberate; old-pose ghosts are removed from these transitions.

129 workspace tests pass (89 sim + 40 client), clippy passes with warnings denied, and locked/offline release passes. Regressions cover legal immediate exits, phase freezing/reset/precedence, source boundaries and low-body/attacker ordering. All 3,240 final ground ticks match baseline byte for byte; focused V4 270 ticks and measured-walk 90 ticks also match their baseline slices. The retained integration trace matches all 2,100 preceding ticks. Its new full 35-second video was played at 1× with 19 temporal samples. Eight fresh smoke images, including versus/training, are byte-identical to the preceding inspected evidence.

The vault review package retains complete before/final videos, traces/cases, 828 baseline and 1,152 final diagnostics, 71 integration diagnostics, smoke images and check logs. Redundant own PNGs were pruned after retaining the evidence; complete final diagnostics were checksum verified in the vault before pruning duplicate remote copies. Exact generation and three edit prompts, original IDs, rejected forward-leaning retreat and gutter corrections are in `crates/client/assets/animation/KOGAN-GROUND-2026-09-05.md`. Remaining Kogan attacks/reactions, Raya's kit and physical stick play stay open.


## Kogan recoil and floor recovery — reviewed

Eight V2 drawings separate standing/crouched hit impact and release from standing/crouched guard brace and release. Four new floor drawings show prone hold, seated hand support, kneel and half rise. Shared anatomical scales keep folded bodies coherent; measured regions preserve full saber/cape and green gutters. The first standing-release candidate leaned too far toward the opponent; a built-in imagegen correction returns the chest toward vertical. Exact creation/edit prompts, originals and measurements are in `crates/client/assets/animation/KOGAN-RECOIL-2026-09-05.md` and `KOGAN-FLOOR-2026-09-05.md`.

Release uses the final four existing stun ticks, 3/2/1/0. A pending knockdown retains impact instead of suggesting regained control. Existing rise/fall drawings follow velocity; four getup phases divide the unchanged 24 frames after the original down hold. Clean reaction cuts and matching-cell trails remove duplicate heads, prone limbs and sabers at transitions. New legal actions immediately take priority. No simulation, input, frame data, trajectory, hitbox, camera or balance changes.

```sh
cargo run --release -p aeon -- --kit-preview --kit-reaction
cargo run --release -p aeon -- --kit-preview --kit-reaction --kit-move=CrK --kit-response=CrouchHit --capture --capture-1x
```

The 36 legal-input cases use Raya as attacker and Kogan as victim: standing P/S hit, crouched K hit, standing/crouching guard, uppercut launch, sweep, normal throw and command grab, both attacker facings at center/corners. Each lasts 150 ticks. Move/response filters apply before `--kit-case`. `--kit-raya` changes the victim but its visual review remains future work. Tests verify real health consequences, guard without damage, both recoil drawings, hitstop freeze, complete launch/down/four-cell getup and return to legal control. A separate regression protects the four visible release ticks, pending knockdown and immediate action priority.

G2/S2 refraction: consequence remains visible after the spark, followed by a recognizable recovery. Aeon's hit opens the chest backward while guard compresses around the hilt; each releases inside its own authored stun. A1's observed tumble/floor/weapon relationship informs hand → knee → foot support, while S1/A2 inform equipment and opponent visibility. These reuse the inspected 1× reference excerpts and paused phases. No reference timings, mechanics or commercial-game parity are claimed.

All 36 before and final cases were viewed completely at 1×, 90 seconds each, in a fixed 1280×720 player with neutral bodies about 210px high. Temporal screenshots cover every facing/corner. Baseline showed a second head during standing recoil, small standing guard, a blade below the getup supports and old limbs over idle. Five focused candidate clips (12.5 seconds) were also viewed; frame steps rejected V1's excessive forward recovery and verified the new floor support.

Final paused checks: StS case 0 ticks 40/42/45/46 show impact, upright release, last held tick and clean idle; CrK crouched hit 24/34/38 shows low impact/release/crouch. Standing guard 33/36/40 and crouching guard 26/28/32 show brace, release and immediate movement/crouch. Uppercut 35/50/56/94 shows rise, fall, full-length prone impact and seated support. Command-grab corner case 2 ticks 77/83/89/95/101 shows every floor/getup phase and clean idle, with both faces and full equipment inside the viewport. The deliberately held prone phase does not add recovery. Air-normal juggle coverage and throw tech remain open; this accepts Kogan's victim response, not Raya's attacking animation.

Final verification: **131 workspace tests (89 sim + 42 client)**, clippy with warnings denied and locked/offline release pass. All 5,400 final reaction ticks match the baseline byte for byte; 750 focused ticks match their baseline slices. The retained 2,100-tick integration trace also matches baseline. Its full new 35-second video was viewed at 1× through all eight scenes. Eight fresh smoke images match the preceding inspected versus/training/title/select/winner evidence byte for byte. Raya's older movement/attack/recoil/getup ghosts and tall ascension effect remain open.

The review archive retains complete before/focused/final videos, traces/cases, 792 baseline and 1,728 final diagnostics, all 750 focused PNGs, 71 integration diagnostics, eight smoke images and check logs. Copies were checksum verified before pruning own redundant remote PNGs; original sources and unrelated captures remain intact. Remaining Kogan attacks, feint/victory/tech and Raya's kit continue under the active goal.

## Kogan Judgment — reviewed

Four built-in imagegen drawings give the grounded dual-weapon rush a compact gather, extended saber/revolver commitment, withdrawal and reholster. V1 corrected the legacy clipped weapons/cape and perpetual baked muzzle flash but left the final revolver dangling. V2's targeted edit broke the saber grip and was rejected before integration. V3 preserves the sword grip and seats the gun in its holster. Exact prompts, originals, rejected candidates and source regions are in `crates/client/assets/animation/KOGAN-JUDGMENT-2026-09-05.md` (vault: `art/fight-ready/animation/`). Identity plates remain unsliced.

Judgment's existing 4 startup / 8 active / 26 recovery, 1000 meter, 280 damage / 24 chip, invulnerability, knockdown and 8 px/f travel remain unchanged. Cell 0 covers startup, cell 1 only active frames, cell 2 the first 18 recovery frames and cell 3 the final eight. Hitstop freezes the selected drawing. Clean cuts and matching-cell trails remove old stance ghosts; legal return immediately supersedes the super drawing. Shared anatomical height 445 preserves head/body scale through the deeper active joint compression. Final roots are 340/1000/420/1040. The withdrawal root places its body about 14 screen pixels rearward within its wide stance, separating faces in standing guard. A narrow draw-order exception keeps a grounded crouched Judgment receiver visible; the full exposed blade and gun remain readable. No simulation, input, hitbox, frame-data, camera or balance changes.

```sh
cargo run --release -p aeon -- --kit-preview --kit-super
cargo run --release -p aeon -- --kit-preview --kit-super --kit-response=CrouchBlock --kit-case=3 --capture --capture-1x
```

The Kogan-only fixture recognizes legal 236236+S from a prepared full bar. Hit, standing block, crouching block and spaced miss × both facings at center/corners give 16 cases of 150 ticks. Response filtering precedes case selection. Tests verify cost, real hit/knockdown, guard/chip, no hit on miss, travel, all four drawings, active-only extension, hitstop freeze and both legal recoveries; draw-order checks cover both player positions and preserve other attacks' ordering.

S2 refraction: the observed SSV Special slash/recoil/withdrawal gives commitment a recognizable end. Aeon's two weapons now extend only during its active interval, then visibly fold and return while the authored glide continues. A2 refraction: GG XX Accent Core's equipment/body/effect relationship motivates complete weapons and readable receivers. The held rush is intentional; no reference timing, attack mechanics or commercial-game parity is claimed. These reuse the inspected normal-speed excerpts and paused phases from the reference dossier.

All 16 baseline, 16 first V3 candidate and 16 corrected final cases were played completely at 1×, 40 seconds per matrix, in the fixed 1280×720 player with neutral bodies about 210px tall. Three focused 2.5s clips were also viewed. Baseline whiff 14/18/35/50/51/54 shows the clipped cape, perpetual extension/flare and old-cape return ghost. V1 miss 15/17/25/43/50/51 exposed the dangling gun. V1 crouching corner guard 25 exposed face occlusion; V3 focused 25/33/51/59 verifies corrected contact, withdrawal and return. Final2 standing guard case 0 and mirrored corner case 3 tick 33 show separated faces. Final2 crouching corner case 3 ticks 25/33 show contact and withdrawal with a readable low guard. Final2 miss case 0 ticks 35/43/50/51 show withdrawal, reholster, last held pose and clean idle. The full saber/cape stays within the viewport and above the floor.

**133 workspace tests (89 sim + 44 client)**, clippy with warnings denied and locked/offline release pass. All 2,400 corrected final ticks equal the baseline byte for byte; the earlier candidate and 450 focused ticks also match their corresponding baseline traces. The final retained integration trace preserves all 2,100 ticks; its complete new 35s video was viewed at 1× through all eight scenes. Smoke verification is recorded separately below. Raya's older movement/attack/recoil/getup ghosts and tall ascension effect remain open.

The vault archive retains complete video/trace/cases, 560 baseline, 560 candidate and 560 corrected final diagnostic PNGs, 450 focused PNGs, 71 final integration diagnostics and check logs. Final accepted gameplay is `judgment-final2/` with `judgment-final2-review.html`; integration is `judgment-polish2/`. Earlier candidates remain for comparison. Complete archives were checksum verified before pruning own redundant remote diagnostics. Capture-space exhaustion interrupted a diagnostic copy; existing source remained intact, complete video/trace/raw frames survived, and diagnostics were recovered before a successful fresh integration capture. Unrelated captures/replays and generated originals remain intact. Remaining Kogan attacks, feint/victory/tech, air juggles and Raya continue under the active goal.

Final smoke: the first timing-dependent run missed title and produced three differing snapshots; its seven images remain as diagnostic evidence. The repeat `judgment-smoke2/` captured all eight required scenes, each byte-identical to the preceding inspected recoil evidence. Versus, training, title, select and winner presentation remain verified.


## Kogan airborne revolver — reviewed

Four selected built-in imagegen drawings give AirShot a compact gather, downward aim, recoil and holstered descent. Approved movement V2 and grounded ranged V5 anchor identity and equipment. The original level gun, missing saber blade, body-scale discontinuity and two-body jump transition are replaced by complete equipment, shared unfolded anatomy and clean cuts. V1 supplies the sound final return; V3 supplies the first three cells. Malformed arm edits are excluded. Exact prompts, original filenames, rejected cells and measured regions are in `crates/client/assets/animation/KOGAN-AIR-SHOT-2026-09-05.md` (vault: `art/fight-ready/animation/`). Source PNGs remain intact; identity plates are not sliced.

The existing 8 startup / 1 active / 12 recovery, one chamber, diagonal 6/−6 velocity, 50 damage / 6 chip and every simulation value remain unchanged. Frames 0–3 gather, 4–8 aim, 9–10 recoil and later attack frames withdraw. The existing landing immediately supersedes the drawing: the low-hop miss attacks on ticks 30–40 and returns to Stand at 41; the full-jump miss attacks on 38–56, lands for 57–58 and stands at 59. The hop deliberately has no room for the longer withdrawal. Shared anatomical height 530 and projected air roots preserve body size while knees fold; full weapons and copper cape remain visible. No extra procedural recoil shifts the drawn muzzle away from the shot.

The air muzzle rays now follow the downward barrel. The projectile tail uses the opposite of the projected velocity in both axes, fixing its previous down/back orientation; level revolver shots retain their established tail length. Neither effect changes projectile position, hitbox or collision. Regression checks cover both facings, unchanged release position, freeze without duplicate flashes, tail direction and clean legal return.

```sh
cargo run --release -p aeon -- --kit-preview --kit-air --kit-move=AirShot
cargo run --release -p aeon -- --kit-preview --kit-air --kit-move=AirShot --kit-jump=full --kit-response=CrouchBlock --kit-case=3 --capture --capture-1x
```

The new Kogan-only `--kit-air` fixture also prepares the six air normals for subsequent review: JP/JK/JS/JHS/JFL/JST plus loaded AirShot, hop/full jump, hit/standing guard/low guard/miss, both facings at center/corners (224 legal-input cases). Empty-cylinder FL reaches JFL. Tests verify actual recognition, guard versus high-normal consequences, projectile chip, spaced misses and grounded recovery. `--kit-jump=hop|full`, move and response filters apply before case selection. Only AirShot's 32 cases are visually accepted in this batch; the six normals remain open.

S1 refraction: SSV Special's observed airborne equipment continuity motivates the complete saber and flowing cape through a weapon choice. S2 gives commitment a recognizable withdrawal; A2 separates the body from its effect, and G1 informs the return to supported landing. Aeon preserves its own quick hop interruption and two-frame full-jump landing. These reuse the dossier's inspected 1× excerpts and paused phases; no commercial gun mechanics, timings or parity are inferred.

All 32 baseline and 32 final cases were played completely at 1×, 80 seconds per matrix in the fixed 1280×720 player, with temporal samples through all facings/corners. Both focused miss clips (5 seconds) were also played. Baseline full miss 38/43/50/57 and hop miss 32/38/40/43 expose the old defects. Focused hop 30/34/38/40/41 and full 38/46/49/56/57/59 verify gather, release, recoil, withdrawal and exact landing. Final mirrored-corner full miss 46/56 verifies muzzle alignment and descent; hop hit 44, standing guard 45 and full low guard 59 verify visible contact and separate bodies. Raya's existing reaction/guard crossfades remain recorded for her upcoming pass.

**137 workspace tests (89 sim + 48 client)**, clippy with warnings denied and locked/offline release pass. All 4,800 final air-gun ticks and 300 focused ticks match their baseline traces. The retained integration trace preserves all 2,100 ticks; its complete new 35-second video was viewed at 1× through all eight scenes. All eight fresh smoke images are byte-identical to the preceding inspected Judgment evidence, retaining verified title/select/versus/training/winner presentation. No simulation or frame-data files changed.

The checksum-verified vault archive retains complete videos/traces/cases, 1,120 baseline and 1,120 final diagnostic PNGs, all 300 focused PNGs, 71 integration diagnostics, eight smoke images and test/build logs. Final footage: `air-final/`, `air-shot-polish/`, `air-shot-smoke/`; players: `air-shot-final-review.html` and `air-shot-polish-review.html`. A sequential PNG spool feeds integration encoding after complete-file verification, preventing capture-space exhaustion while retaining the complete video/trace and selected frames. Generated originals and unrelated captures/replays remain intact. Remaining Kogan attacks/tech/feint/victory/air juggles and Raya continue under the active full-kit goal.


## Airborne saber batch — reviewed

Six V2 drawings distinguish Kogan JS's compact downward cut, JHS's extended diagonal and JST's steep close line. They share a gathered preparation, withdrawal and ready descent. The former shared drawing showed a clipped broad crescent from startup through recovery; the new full blade communicates each attack's actual direction and relative reach. The copper cape remains large behind the body. Shared unfolded anatomical scale and measured roots remove the old airborne shrink; clean cuts and matching-cell trails remove duplicate bodies. V1's shortened gathering blade was rejected, and V2 corrects its length. Exact prompts, original image IDs, regions and roots are in `crates/client/assets/animation/KOGAN-AIR-SABER-2026-09-05.md` (vault art archive has both intact originals).

The existing JS 5/6/6 and JHS/JST 6/5/8 timing, High classification, geometry, damage and input rules are unchanged. Only existing active frames use the contact drawing. Recovery withdraws, with the last three existing attack ticks preparing to land. An expired air cut keeps ready descent in spent-air-action Jump. Recent drawing history selects the existing front-saber landing pose for the original two full-jump landing ticks, then yields immediately to control. This avoids the candidate's brief rear-facing blade at touchdown and cannot affect later standalone jumps. The low hop intentionally lands before these longer recovery drawings; no time is added to display them.

```sh
cargo run --release -p aeon -- --kit-preview --kit-air --kit-move=JHS
cargo run --release -p aeon -- --kit-preview --kit-air --kit-air-early --kit-move=JST --kit-jump=full
cargo run --release -p aeon -- --kit-preview --kit-air --kit-move=JS --kit-response=StandBlock --kit-case=3 --capture --capture-1x
```

The ordinary fixture supplies 96 saber cases: three moves, hop/full jump, hit/standing guard/crouching guard/miss, both facings at center/corners. The new `--kit-air-early` fixture supplies 24 additional saber misses with legal apex input to expose withdrawal above the floor. It also supports the three remaining air normals for subsequent review; there are 48 early cases across those six normals. Ordinary inputs are unchanged. These High normals defeat low guard under the existing rules; the response label describes the defender's input, not a successful block.

S1 refraction: SSV Special's observed airborne blade/cloth distinction becomes a complete cyan saber separated from Kogan's copper cape. S2's committed extension and visible withdrawal become different drawings inside Aeon's existing attack duration. A2's equipment/effect hierarchy motivates removing the perpetual opaque crescent so the opponent and actual blade remain readable. G1's changing leg arrangement and recognizable grounded return inform ready descent and the front-saber touchdown. These reuse the inspected 1× excerpts and paused phases in the reference dossier; Aeon's compact hop interruption, two-tick full landing and exact move values remain intentional differences. The sources do not establish Aeon's mechanics or commercial parity.

Before review: all 32 ordinary JS cases / 80s were viewed at 1× at 1280×850; the initial narrow hop review was repeated completely at that size. JHS/JST ordinary hop/full misses were viewed for 16 cases / 40s at 1280×720, and all three full-jump early-miss clips for 12 cases / 30s at 1280×850. Other recorded baseline contact clips are archived without a claim of complete playback. Legacy JS full miss ticks 51/56/58 expose early extension, clipping and the old transition. All twelve focused candidate clips / 30s were viewed at 1×, including JST full early 39/45/50/55/57; the rear-blade touchdown found there was corrected before the final capture.

All **120 final cases / 300 seconds** were then played completely at 1× with temporal samples through both facings and corners. JS used 1280×850; JHS/JST used 1280×720 with neutral bodies about 210px tall, matching the gameplay-sized baseline and dossier comparisons. Final JS early full ticks 55/56/57/58/59 show spent-air descent, two front-saber landing ticks and clean stance. JHS early full 44/49/54/57 show extension, withdrawal, ready and touchdown; JST 45/50/55/56/57 show the steep blade and matching recovery. Complete weapons remain above the floor, inside the viewport and below the HUD. Raya's older reaction/guard/getup ghosts remain separately open; their presence is not accepted as completed Raya coverage.

**140 workspace tests (89 sim + 51 client)** pass, with clippy warnings denied and locked/offline release. Regression checks cover active-only selection, freeze, legal recovery, attack expiry, later-jump/reset isolation, source boundaries and clean transitions. All **18,000 final ticks** equal the corresponding pre-art baseline; all 1,800 focused ticks also match. The retained integration preserves all **2,100 ticks**, and its complete new **35s** video was viewed at 1×. Eight new smoke hashes match the preceding inspected airborne-gun evidence, covering title/select/versus/training/winner. Simulation and frame-data sources are unchanged.

Evidence lives under `notes/media/2026-09-05-full-kit/`: `air-before/`, `air-saber-focused/`, `air-final/`, `air-saber-polish/`, and `air-saber-smoke/`. The archive retains all complete videos/traces/case indexes, 4,200 final diagnostic PNGs, 1,800 focused PNGs, 71 integration diagnostics, eight smoke images and check logs. Review players are `air-saber-{JS,JHS,JST}-final-review.html` and `air-saber-integration-review.html`. Archives were checksum verified before pruning redundant goal-owned remote PNGs. Partial baseline attempts with `-failed-space` names remain explicitly excluded; fresh retries succeeded. Remaining Kogan normals/throws/tech/feint/victory/air juggles and Raya continue under the full-kit goal.

## Kogan airborne fist, boot and knee

Six selected drawings replace the shared legacy AirLight pose for JP, JK and empty-cylinder JFL. V1 supplies the gathered preparation, withdrawal and ready descent; V4 supplies separate downward fist, extended boot and bent knee contacts. Gameplay review rejected V1's contact height: all three limbs passed above crouched Raya while hits registered. V2's fist correction remained too high on source inspection. V3 lowered fist and boot, and V4 lowered the knee. The corrected drawings retain the full rear cyan saber, large copper cape and consistent unfolded body scale. Intact source atlases and exact prompts are recorded in `crates/client/assets/animation/KOGAN-AIR-LIGHTS-2026-09-05.md`.

Existing JP/JK 4/8/4 and JFL 5/6/6, High classification, geometry, damage, gauge and input rules remain unchanged. Only existing active frames select the contact drawing. Recovery folds the limbs, with the last two attack ticks preparing descent; an expired attack retains ready descent while the air action is spent. Every grounded or new action immediately supersedes this selection. Ordinary low attacks land before the longer withdrawal; early full-jump misses expose the complete four-phase action without adding recovery. Clean cuts and matching-cell trails remove the legacy two-body transition. The rear saber remains compatible with the reviewed movement landing.

```sh
cargo run --release -p aeon -- --kit-preview --kit-air --kit-move=JP
cargo run --release -p aeon -- --kit-preview --kit-air --kit-move=JK --kit-response=CrouchBlock --kit-case=3 --capture --capture-1x
cargo run --release -p aeon -- --kit-preview --kit-air --kit-air-early --kit-move=JFL --kit-jump=full
```

K2 refraction: the inspected KOF XIII leg chamber, extension and replacement motivate separate gathered, boot-contact and withdrawal keys. G2's observable arm extension and return motivate a distinct left-fist line and elbow withdrawal; G1 informs the changing legs and supported landing. Aeon's airborne timing intentionally differs from these grounded examples, with immediate hop landings and brief held keys. The before/after comparison judges readable gesture and contact inside Aeon's timing, not copied mechanics or commercial-game parity.

Baseline review limits: all 40 JP cases / 100 seconds played at 1×; JK and JFL each had hop/full ordinary misses and full early misses viewed (12 cases / 30 seconds per move). JP full early 39/43/51/55 was stepped, confirming old wrong leg contact, missing withdrawal and duplicate jump body. All 120 baseline cases are archived. Rejected V1 had all 120 cases captured, but only its first three hop response clips per move were completely viewed (36 cases / 90 seconds). Its six focused early misses were also played (15 seconds), with JFL full 39/44/49/55/57 stepped. These miss-only clips did not establish contact quality.

Corrected V4 focused CrouchBlock: all six center-right hop/full clips / 15 seconds played at 1× in 1280×720, with contact samples. JP, JK and JFL visibly meet the crouched body. JFL full 56/64/65/67 stepped through frozen contact, post-freeze contact, legal two-tick landing and Stand. All 900 focused ticks match the corresponding baseline slices.

All **120 corrected final cases / 300 seconds** were played completely at 1× in the 1280×720 player: JP, JK and JFL each have 40 cases / 100s, covering hop/full jump, hit, standing guard, defeated low guard, ordinary miss and early miss, both facings at center/corners. Four temporal screenshots sampled each ten-second clip. Full early JP 39/43/51/55, JK 43/51 and JFL 39/44/49/55 were stepped. The bent knee remains distinct from the longer boot, corrected limbs meet both standing and crouched bodies, and full weapons/cape remain visible through coherent withdrawal and legal returns. The focused JFL landing steps account for hitfreeze: ticks 65–66 are the two landing ticks, then Stand at 67. The ordinary no-contact full jump still lands at 57–58 and stands at 59. Raya's older reaction/guard transitions remain open for her pass.

**142 workspace tests (89 sim + 53 client)**, clippy with warnings denied and locked/offline release pass. Tests cover active-only contacts, freeze, full early recovery, attack expiry, new-state isolation, source boundaries and clean transitions. All **18,000 corrected final ticks** equal the corresponding pre-art baseline; all 900 focused contact ticks also match. The retained integration preserves all **2,100 ticks**, and its complete new **35s** video was viewed at 1× with temporal samples through all eight scenes. Eight fresh smoke PNGs are byte-identical to the preceding inspected airborne-saber evidence. No simulation, move data, input or frame-data files changed.

The checksum-verified vault archive under `notes/media/2026-09-05-full-kit/` retains complete videos, traces and case indexes: `air-before/`, rejected V1 `air-final/JP-*`, `JK-*`, `JFL-*`, `air-lights-focused/`, `air-lights-contact-v4/`, corrected `air-final-lights-v4/`, `air-lights-polish/`, and `air-lights-smoke/`. It includes 4,200 corrected final diagnostics, all 900 focused contact PNGs, 71 integration diagnostics, eight smoke images and check logs (`air-lights-tests.log`, `air-lights-clippy.log`, final asset/client/release `air-lights-v4-check.log`). Players: `air-lights-{JP,JK,JFL}-v4-review.html`, `air-lights-contact-v4-review.html`, and `air-lights-integration-review.html`. Full archives were checksum verified before each goal-owned remote clip was reduced to eight diagnostic PNGs; originals and unrelated captures/replays remain intact. Remaining Kogan grounded normals/throws/tech/feint/victory/air juggles and Raya continue under the full-kit goal. The original blocked grounded StP/StK/CrK art request has not been retried.

## Kogan standing Flash and Style

Seven selected V2 drawings give StFL a short low pommel gesture and StST a longer waist-level saber line. Each has preparation, contact, withdrawal and ready phases; both deliberately share the sound down-forward withdrawal. The pommel interpretation follows Kogan's previous FL picture and the existing short frame-trap role. It adds no mechanic. V1's high Style line was corrected in V2. V1/V2's pommel-return tip and V3's overlong correction are excluded; intact originals and exact prompts remain in the art archive, with portable provenance in `crates/client/assets/animation/KOGAN-FLASH-2026-09-05.md`.

The previous pictures held the same extended arm and, for Style, a cyan crescent from preparation through recovery, above crouched contact. The selected drawings lower the visible weapon to its contact line, keep the complete saber and large copper cape, and show a bent arm bringing the weapon home. Shared anatomical height 310 and measured floor roots preserve scale and support. Clean cuts and matching-cell trails remove the old stance ghost; additional procedural rotation, scale and shifts are suppressed for this drawn commitment.

StFL remains 7 startup / 3 active / 12 recovery, Mid, 75 damage, x8/y30/w54/h24. StST remains 9/3/14, Mid, 90 damage, x12/y40/w60/h22. Contact appears only during the existing active ticks; withdrawal occupies the first half of recovery, then ready. The original no-contact returns remain StFL tick 35 and StST tick 39 in this fixture. Hitstop holds the selected phase; new states immediately own the body. Simulation, move data, input and frame-data files are unchanged.

```sh
cargo run --release -p aeon -- --kit-preview --kit-flash
cargo run --release -p aeon -- --kit-preview --kit-flash --kit-move=StFL --kit-response=CrouchHit
cargo run --release -p aeon -- --kit-preview --kit-flash --kit-move=StST --kit-response=Whiff --kit-case=3 --capture --capture-1x
```

The new fixture supplies 40 legal cases: two moves, hit/standing guard/crouching guard/crouched hit/miss, both facings at center/corners. Tests establish actual recognition, each response's hit/block outcome, all four phases, frozen contact and actionable grounded recovery. The prior lights fixture keeps its original cases and inputs.

S2 refraction: the inspected SSV Special extension, contact hold and visible weapon withdrawal motivate a clear committed saber line followed by a different return drawing. G2's observable outbound arm and return to guard motivate supported hip/arm changes beneath the compact pommel gesture. Aeon retains its existing short timings and copper/cyan identity; no commercial timings, mechanics or parity are inferred. The two moves share a withdrawal because both finish by returning the same right-hand saber to ready.

All 40 baseline cases / 40 seconds were played completely at 1× in the 1280×720 player, with temporal samples through all facings and corners. Baseline StFL miss 14/20/27/33 and StST miss 17/22/28/37 were stepped. All four focused case-0 clips / 4 seconds were played at 1×; exact crouched contacts StFL 20 and StST 22 were paused. Focused StFL miss 14/20/27/33/35 and StST 17/22/28/37/39 show preparation, commitment, shared withdrawal, ready and original Stand. Step presses lost during initial media loading were repeated after verifying loaded time and the in-game frame label. The focused traces preserve all 240 ticks.

All **40 final cases / 40 seconds** were played completely at 1× in 1280×720, with four temporal screenshots per clip. Each move covers all five responses, both facings at center/corners. Standing hit contact was also paused at case-local tick 20 for StFL and 22 for StST across all four facing/position combinations. The short pommel and longer horizontal blade remain distinct and meet the standing/crouched body. Full equipment stays inside the viewport, the withdrawal clears the floor, and both return on their original ticks. Raya's existing reaction/guard ghosts and the broader old/new style transition remain on the full-kit ledger; this accepts the two Kogan attack sequences only.

**144 workspace tests (89 sim + 55 client)**, clippy with warnings denied and locked/offline release pass. All **2,400 final ticks**, **240 focused ticks** and the retained **2,100 integration ticks** equal their pre-art baselines. The complete new **35s integration** played at 1× with temporal screenshots through all eight scenes. Eight fresh smoke PNGs are byte-identical to the preceding inspected air-light evidence. Final code review and whitespace checks pass; no simulation, move-data, input or frame-data files changed.

The checksum-verified vault archive under `notes/media/2026-09-05-full-kit/` retains `flash-before/`, `flash-focused/`, `flash-final/`, `flash-polish/` and `flash-smoke/`: complete videos, traces and case indexes, 1,080 baseline and 1,080 final PNG diagnostics, all 240 focused PNGs, 71 integration diagnostics, eight smoke images and `flash-check.log`. `flash-verification.json` records the comparisons. Players are `flash-{StFL,StST}-{before,final}-review.html`, `flash-focused-review.html` and `flash-integration-review.html`. Each full clip archive was checksum checked before its goal-owned remote diagnostic set was reduced to eight files. Originals and unrelated captures remain intact. Remaining Kogan crouching normals, overheads, throw/tech/feint/victory/air juggles and Raya continue under the active goal; the original blocked StP/StK/CrK request has not been retried.
